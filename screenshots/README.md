# Screenshot Notes

We do **not** commit `.png` screenshot binaries to git anymore.

To capture and share a screenshot in chat:
1. Start the app with `npm run dev`.
2. Run `npm run screenshot -- http://127.0.0.1:3000 screenshots/latest-homepage.png`.
3. Share that file directly in chat (without committing it).
